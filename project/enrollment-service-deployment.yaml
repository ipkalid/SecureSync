apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.33.0 (HEAD)
  labels:
    io.kompose.service: enrollment-service
  name: enrollment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: enrollment-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml
        kompose.version: 1.33.0 (HEAD)
      labels:
        io.kompose.network/project-default: "true"
        io.kompose.service: enrollment-service
    spec:
      containers:
        - env:
            - name: EX_PORT
              value: "8085"
          image: gcr.io/mdm-mx/enrollment-service:latest
          imagePullPolicy: Always
          name: enrollment-service
          ports:
            - containerPort: 80
              hostPort: 8085
              protocol: TCP
      restartPolicy: Always
