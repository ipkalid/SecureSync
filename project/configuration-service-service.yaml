apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.33.0 (HEAD)
  labels:
    io.kompose.service: configuration-service
  name: configuration-service
spec:
  ports:
    - name: "8087"
      port: 8087
      targetPort: 80
  selector:
    io.kompose.service: configuration-service
